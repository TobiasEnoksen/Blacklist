<%= form_with(model: movie, local: true) do |form| %>
  <% if movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <h4>Title:</h4>
    <%= form.text_field :title, class: "form-control input-lg" %>
  </div>

  <div class="form-group">
    <h4>Year:</h4>
    <%= form.text_field :year, class: "form-control input-lg" %>
  </div>

  <div class="form-group">
    <h4>Description:</h4>
    <%= form.text_field :desc, class: "form-control input-lg" %>
  </div>
  
  <div class="form-group">
    <%= form.label :image %>
    <%= form.text_field :image, class: "form-control input-lg" %>
  </div>

  <div class="form-group">
    <%= form.submit 'Create Movie', class: "btn btn-danger btn-lg" %>
  </div>
<% end %>
